<h1>{{title}}</h1>
{{# if products }}
<h1><strong>Thanks For Shopping With Us!!</strong></h1>
<div class="cart page">
    <ul class="list-group">
        {{# each products }}
            {{ formatDate order_date}}
                {{# each Products}}
                    <li class="list-group-item clearfix">
                        <div class="col-sm-5">
                            <strong>{{ product_name }}</strong>
                            <br/>
                            <img class="prodimg" src={{product_image}} class="rounded img-fluid" width="200rem">
                        </div>
                        <br>
                        <div class="col-sm-2 text-right mt-4" >
                        <label>Quantity</label>
                        </br>
                            {{ OrderProduct.order_quantity }}
                        </div>
                        <div class="col-sm-2 text-right mt-4" >
                         <label>Total Amount</label>
                        </br>
                            ${{ formatTotal price OrderProduct.order_quantity }}
                        </div>
                    </li>
                {{/each}}
        {{/each}}
    </ul>
</div>
<div class="text-right">
    <strong>Order Total: ${{ total }} </strong>
</div>
<hr>

{{ else }} You have no orders !! {{/if}}